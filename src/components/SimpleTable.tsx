import React from 'react';export function SimpleTable({rows}:{rows:Record<string,any>[]} ){if(rows.length===0)return <div className='card caption'>데이터 대기</div>;const headers=Object.keys(rows[0]);return(<div className='card'><table className='table'><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=>(<tr key={i}>{headers.map(h=><td key={h}>{String(r[h]??'')}</td>)}</tr>))}</tbody></table></div>)}